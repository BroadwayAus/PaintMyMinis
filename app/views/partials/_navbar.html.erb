<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <a class="navbar-brand" href="/"><%= image_tag "logo_final" %></a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNav">
    <ul class="navbar-nav">
    <%#Multiple if statements to ensure that the current nav options are available depending on if the user is currently logged in%>
        <li class="nav-item active nav_btn">
        <%= link_to "Home", root_path, class: "nav-link" %>
        </li>
        <li class="nav-item active nav_btn">
        <% if user_signed_in? == false %>
        <%= link_to "Sign In", new_user_session_path, class: "nav-link" %>
        <% end %>
      </li>
      <li class="nav-item active nav_btn">
        <% if user_signed_in? %>
        <%= link_to "Browse", item_page_path, class: "nav-link" %>
        <% end %>
      </li>
        <li class="nav-item active nav_btn">
        <% if user_signed_in? %>
        <%= link_to "Post", new_listing_path, class: "nav-link" %>
        <% end %>
      </li>
        <li class="nav-item active nav_btn">
        <% if user_signed_in? %>
        <%= link_to "Sign out", destroy_user_session_path, method: :delete, class: "nav-link" %>
        <% end %>
      </li>
    </ul>
  </div>
</nav>